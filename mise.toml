[tools]
pnpm = "12.6.0"
node = "24.21.0"
actionlint = "1.7.12"
shellcheck = "0.11.0"
zizmor = "1.30.1"
gitleaks = "8.30.1"
lefthook = "2.1.14"
taplo = "0.10.0"

[tasks.setup]
description = "環境基盤を最新化"
run = ["mise install", "lefthook install", "pnpm install --frozen-lockfile"]

[tasks.yamllint]
description = "YAML を検証"
raw_args = true
run = "uvx --python-preference only-managed yamllint==1.38.0"
